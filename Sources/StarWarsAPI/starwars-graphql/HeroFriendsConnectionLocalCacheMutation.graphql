query HeroFriendsConnectionLocalCacheMutation($id: ID!, $first: Int!, $after: ID) @apollo_client_ios_localCacheMutation {
  character(id: $id) {
    id
    name
    friendsConnection(first: $first, after: $after) {
        friends {
            id
            name
        }
        totalCount
        pageInfo {
            hasNextPage
            endCursor
        }
    }
  }
}
